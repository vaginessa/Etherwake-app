<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="SettingsTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/PurbleActionBar</item>
        <item name="actionBarStyle">@style/PurbleActionBar</item>
    </style>

    <style name="PurbleActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/purble</item>
        <item name="background">@color/purble</item>
    </style>

</resources>
